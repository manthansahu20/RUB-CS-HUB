#include<stdio.h>
 void main()
{
  int num;
  printf("enter the value pf a:");
  scanf("%d",&a);
  if(num == 0)
    {
      printf("%d is not an odd and even number");
    }
  else if(num % 2==0)
    {
      printf("%d is an even number");
  else 
    {
      printf("%d is an odd number");
    }
}

